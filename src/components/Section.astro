---
interface Props {
  id?: string;
  label?: string;
  class?: string;
}

const { id, label, class: className } = Astro.props;
---

<section id={id} class:list={["section", className]}>
  <div class="container">
    {label && <h2 class="mono section-label">{label}</h2>}
    <slot />
  </div>
</section>

<style>
  .section {
    padding-block: var(--section-pad);
    scroll-margin-top: 64px; /* clear the sticky nav on anchor jumps */
  }

  .container {
    max-width: var(--container);
    margin-inline: auto;
    padding-inline: var(--container-pad);
  }

  .section-label {
    margin: 0 0 1.75rem;
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-faint);
  }
</style>
